# Run Report

Status: landed

This smoke run is non-actionable. The later landed verification superseded the
work, and the smoke branch points at a commit already contained in the
current project history.

The registered worktree remains so a future cleanup command can remove
historical worktrees safely while preserving the superseded reason in
Factory run artifacts.
